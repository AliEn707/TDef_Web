// will used for getting new messages by loading <script>
<% if (@messages.size!=0) then%>
	timestamp="<%= @messages[-1].created_at.to_i+1 %>"
	messages=<%=render("messages").inspect.html_safe%>
	addMessages();
<% end %>
